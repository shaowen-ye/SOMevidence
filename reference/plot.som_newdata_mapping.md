# Plot new-data mapping shift across an ensemble

Plot new-data mapping shift across an ensemble

## Usage

``` r
# S3 method for class 'som_newdata_mapping'
plot(x, ...)
```

## Arguments

- x:

  A `som_newdata_mapping` object.

- ...:

  Unused.

## Value

An editable `ggplot` object.
