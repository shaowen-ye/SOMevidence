# Plot mapping shift in held-out domains

Plot mapping shift in held-out domains

## Usage

``` r
# S3 method for class 'som_transfer_audit'
plot(x, ...)
```

## Arguments

- x:

  A `som_transfer_audit` object.

- ...:

  Unused.

## Value

A `ggplot` object.
