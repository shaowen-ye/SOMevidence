# Plot partition reproducibility across candidate class counts

Plot partition reproducibility across candidate class counts

## Usage

``` r
# S3 method for class 'som_partitions'
plot(x, ...)
```

## Arguments

- x:

  A `som_partitions` object.

- ...:

  Unused.

## Value

A faceted `ggplot` object with ensemble agreement intervals.
