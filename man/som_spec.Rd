% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{som_spec}
\alias{som_spec}
\title{Specify a reproducible SOM ensemble}
\usage{
som_spec(
  grids = data.frame(xdim = 7L, ydim = 5L),
  seeds = 1:10,
  rlen = 500L,
  alpha = c(0.05, 0.01),
  radius = NULL,
  topology = c("hexagonal", "rectangular"),
  neighbourhood = c("gaussian", "bubble"),
  toroidal = FALSE,
  mode = c("batch", "online", "pbatch"),
  k = 2:8,
  max_na_fraction = 0,
  layer_weights = NULL,
  normalize_layers = TRUE,
  cores = 1L
)
}
\arguments{
\item{grids}{A two-column data frame with \code{xdim} and \code{ydim}, a numeric
vector of length two for one grid, or a list of such vectors.}

\item{seeds}{Integer random seeds.}

\item{rlen}{Number of training iterations.}

\item{alpha}{Initial and final learning rates.}

\item{radius}{Optional initial and final neighbourhood radii.}

\item{topology}{Rectangular or hexagonal grid.}

\item{neighbourhood}{Bubble or Gaussian neighbourhood.}

\item{toroidal}{Whether the grid is toroidal.}

\item{mode}{Training mode supported by \code{\link[kohonen:supersom]{kohonen::supersom()}}.}

\item{k}{Candidate numbers of hard partitions retained for later auditing.}

\item{max_na_fraction}{Maximum allowed fraction of missing variables per
layer and sample passed to \code{kohonen}.}

\item{layer_weights}{Optional non-negative scientific weights, one per layer.}

\item{normalize_layers}{Whether to divide each scientific layer weight by
its training-set mean squared Euclidean distance. The package uses the
variance identity for a deterministic, leakage-safe estimate and applies
the same effective weights to SOM and cross-model fits.}

\item{cores}{Number of cores used by \code{kohonen} in parallel batch mode.}
}
\value{
A \code{som_spec} object.
}
\description{
Specify a reproducible SOM ensemble
}
\examples{
specification <- som_spec(
  grids = list(c(4, 3), c(5, 4)), seeds = 1:2, rlen = 50, k = 2:4
)
expand_som_spec(specification)
}
