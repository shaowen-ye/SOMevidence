% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gates.R
\name{assess_defensibility}
\alias{assess_defensibility}
\title{Assess partition defensibility without forcing a verdict}
\usage{
assess_defensibility(
  audit,
  partitions = NULL,
  k = NULL,
  gate = NULL,
  consensus = NULL,
  cross_model = NULL
)
}
\arguments{
\item{audit}{A \code{som_audit} object.}

\item{partitions}{Optional \code{som_partitions} object. At least one partition-
derived evidence object is required for a \code{"supported"} decision.}

\item{k}{Candidate number of clusters when partition stability is assessed.}

\item{gate}{Optional analyst-defined \code{som_gate}. Without one the status is
\code{"not_assessed"}.}

\item{consensus}{Optional \code{som_consensus} object for the same \code{k}.}

\item{cross_model}{Optional \code{som_cross_comparison} object.}
}
\value{
A \code{som_defensibility} object containing evidence and reasons.
}
\description{
In addition to analyst-defined thresholds, the assessment checks that every
source partition and the consensus retain the requested \code{k}, and that all
requested cross-model fits succeeded. Missing audit evidence produces an
uncertain result rather than being treated as a pass.
}
\details{
A \code{"supported"} status means only that the analyst-specified evidence
requirements and structural completeness checks were met for the stated
data, analysis design, preprocessing, model budget and candidate \code{k}. It does
not demonstrate that discrete ecological types exist, identify a causal
mechanism, or establish ecological truth.

A representation audit alone cannot support a hard partition. A supported
decision also requires traceable comparative partition evidence and at
least one prespecified partition-quality requirement. Missing evidence or a
representation-only gate produces an \code{"uncertain"} result.
}
