% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_som_ensemble}
\alias{fit_som_ensemble}
\title{Fit a reproducible ensemble of self-organizing maps}
\usage{
fit_som_ensemble(
  data,
  spec,
  resamples = NULL,
  preprocess = som_preprocess(),
  keep_models = TRUE,
  fail_fast = FALSE,
  parallel = FALSE
)
}
\arguments{
\item{data}{A \code{som_data} object.}

\item{spec}{A \code{som_spec} object.}

\item{resamples}{A \code{som_resamples} object. The default fits the full data.}

\item{preprocess}{One \code{som_preprocess} object or a named object per layer.}

\item{keep_models}{Whether to retain fitted \code{kohonen} objects.}

\item{fail_fast}{Whether to stop at the first model failure.}

\item{parallel}{Whether to distribute ensemble members through the current
\code{future} plan using \code{future.apply}. The package never changes that plan.
Explicit model seeds retain reproducibility across sequential and future
execution. Avoid combining worker-level parallelism with \code{spec$cores > 1}
unless nested parallelism is intended.}
}
\value{
A \code{som_ensemble} object.
}
\description{
Every model receives preprocessing parameters estimated only from its own
analysis rows. Failures are recorded instead of silently discarded.
Topographic error is computed during fitting so duplicate full processed
matrices need not be retained in every ensemble member.
}
\examples{
data <- simulate_som_scenario("clusters", n = 45, p = 3, seed = 1)
specification <- som_spec(c(3, 2), seeds = 1:2, rlen = 10, k = 2)
ensemble <- fit_som_ensemble(data, specification, keep_models = FALSE)
ensemble
}
