% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_som_scenario}
\alias{simulate_som_scenario}
\title{Simulate known-structure data for SOM validation}
\usage{
simulate_som_scenario(
  scenario = c("gradient", "clusters", "overlap", "grouped_pseudoreplication",
    "multilayer_conflict"),
  n = 180L,
  p = 6L,
  seed = 1L,
  noise_sd = 1,
  class_probs = c(1, 1, 1),
  n_groups = NULL,
  group_icc = 0.7,
  n_domains = 3L,
  domain_shift = 0,
  missing_rate = 0,
  missing_mechanism = c("none", "mcar", "domain"),
  id = NULL
)
}
\arguments{
\item{scenario}{One of \code{"gradient"}, \code{"clusters"}, \code{"overlap"},
\code{"grouped_pseudoreplication"} or \code{"multilayer_conflict"}.}

\item{n}{Number of samples.}

\item{p}{Number of variables per primary layer.}

\item{seed}{Random seed.}

\item{noise_sd}{Within-structure residual standard deviation.}

\item{class_probs}{Three positive class proportions for simulations with
genuine discrete classes. They are normalized internally. Ignored for a
continuous gradient.}

\item{n_groups}{Number of sampling groups for
\code{"grouped_pseudoreplication"}. The default is approximately one group per
six observations, with a minimum of 10 groups.}

\item{group_icc}{Target intraclass correlation induced by the simulated
group effect. Used only for \code{"grouped_pseudoreplication"}.}

\item{n_domains}{Number of balanced sampling domains. A gradient always has
domains spanning its latent coordinate. Other scenarios receive domains
when \code{domain_shift > 0} or \code{missing_mechanism = "domain"}.}

\item{domain_shift}{Magnitude of an additive domain-specific shift applied
to the first two variables. Zero creates no additional domain shift.}

\item{missing_rate}{Expected fraction of entries set to missing.}

\item{missing_mechanism}{One of \code{"none"}, \code{"mcar"} or \code{"domain"}. Domain
missingness is concentrated in the final sampling domain.}

\item{id}{Optional stable sample identifiers. Supply these when independently
constructed simulated objects will be compared in a sensitivity workflow.}
}
\value{
A \code{som_data} object with additional \code{truth} and \code{simulation_spec}
components. Known latent variables and labels are never included in the
training layers. Discrete labels are also copied to \code{external_label} for
post hoc agreement assessment.
}
\description{
Simulate known-structure data for SOM validation
}
\section{Lifecycle}{

Experimental. Existing scenario names are retained during candidate v0.1
testing, but the simulation catalogue and scenario-specific controls may
expand before the first stable release.
}

