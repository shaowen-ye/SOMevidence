% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross-model.R
\name{fit_cross_models}
\alias{fit_cross_models}
\title{Fit controlled cross-model reference partitions}
\usage{
fit_cross_models(
  ensemble,
  methods = c("kmeans", "ward", "gmm"),
  k = ensemble$spec$k,
  kmeans_seeds = 1L,
  kmeans_nstart = 50L,
  kmeans_iter_max = 100L,
  gmm_model_names = NULL,
  keep_models = FALSE,
  fail_fast = FALSE
)
}
\arguments{
\item{ensemble}{A fitted \code{som_ensemble} defining data, preprocessing and
resampling.}

\item{methods}{Any of \code{"kmeans"}, \code{"ward"} and \code{"gmm"}.}

\item{k}{Candidate numbers of clusters.}

\item{kmeans_seeds}{Random seeds used only for K-means initialization.}

\item{kmeans_nstart}{Number of random starts per K-means fit.}

\item{kmeans_iter_max}{Maximum number of iterations per K-means start.}

\item{gmm_model_names}{Optional covariance models passed to
\code{\link[mclust:Mclust]{mclust::Mclust()}}. BIC selection occurs within each analysis split.}

\item{keep_models}{Whether to retain fitted cross-model objects.}

\item{fail_fast}{Whether to stop at the first fit failure.}
}
\value{
A \code{som_cross_models} object with partitions, warnings and explicit
failures.
}
\description{
K-means, Ward.D2 and Gaussian mixture models use the same data object,
resampling indices and leakage-safe preprocessing as a fitted SOM ensemble.
The function creates reference partitions for triangulation; it does not
rank algorithms or define any method as ground truth.
}
